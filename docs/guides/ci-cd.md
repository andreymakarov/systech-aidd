# CI/CD кратко

Текущее состояние: в репозитории CI/CD не настроен.

## Локальная проверка качества
- `make qa` — форматирование и линтинг.
- `make test` — тесты с покрытием.

## Конфигурация инструментов
- Настройки ruff, mypy, pytest/coverage — в `pyproject.toml`.

