# План разработки LLM-ассистента

## 📊 Прогресс разработки

| Итерация | Функционал | Статус | Дата |
|----------|-----------|--------|------|
| 1 | Настройка проекта и конфигурация | ✅ Завершено | 2025-10-10 |
| 2 | Базовый Telegram бот | ✅ Завершено | 2025-10-10 |
| 3 | Интеграция с LLM | ✅ Завершено | 2025-10-10 |
| 4 | Управление историей диалогов | ✅ Завершено | 2025-10-10 |
| 5 | Команда очистки истории | ✅ Завершено | 2025-10-10 |
| 6 | Обработка ошибок и финальная полировка | ✅ Завершено | 2025-10-10 |
| 7 | Ролевая модель с командой /role (TDD) | ⏳ Ожидает | - |

**Легенда:**
- ⏳ Ожидает
- 🚧 В работе
- ✅ Завершено
- ❌ Заблокировано

---

## Итерация 1: Настройка проекта и конфигурация

**Цель:** Подготовить окружение и создать класс конфигурации

**Задачи:**
- [x] Создать `pyproject.toml` с зависимостями (aiogram, openai, python-dotenv)
- [x] Создать структуру директорий `src/bot/`
- [x] Создать `.env.example` с шаблоном конфигурации
- [x] Создать `.gitignore` (исключить `.env`, `__pycache__`, `.venv`)
- [x] Создать `src/bot/config.py` с классом `Config`
- [x] Создать `Makefile` с командами `install`, `run`, `clean`

**Тест:** `make install` устанавливает зависимости без ошибок. `Config` загружает переменные из `.env` и падает при отсутствии обязательных параметров.

---

## Итерация 2: Базовый Telegram бот

**Цель:** Запустить бота, который отвечает на команду `/start`

**Задачи:**
- [x] Создать `src/bot/__init__.py`
- [x] Создать `src/bot/bot.py` с классом `TelegramBot`
- [x] Создать `src/bot/handlers.py` с базовым `MessageHandler`
- [x] Реализовать обработчик команды `/start`
- [x] Создать `src/bot/__main__.py` для запуска приложения
- [x] Настроить базовое логирование

**Тест:** `make run` запускает бота. Команда `/start` в Telegram возвращает приветственное сообщение.

---

## Итерация 3: Интеграция с LLM

**Цель:** Бот отвечает на текстовые сообщения через LLM

**Задачи:**
- [x] Создать `src/bot/llm_client.py` с классом `LLMClient`
- [x] Реализовать метод `generate_response()` через OpenAI client
- [x] Добавить обработчик текстовых сообщений в `MessageHandler`
- [x] Интегрировать `LLMClient` с обработчиком сообщений
- [x] Добавить typing action ("печатает...")

**Тест:** Отправка текстового сообщения боту возвращает ответ от LLM. Без истории диалога (каждое сообщение независимо).

---

## Итерация 4: Управление историей диалогов

**Цель:** Бот ведет контекст диалога для каждого пользователя

**Задачи:**
- [x] Создать `src/bot/conversation.py` с классом `ConversationManager`
- [x] Реализовать методы `get_history()`, `add_message()`
- [x] Интегрировать историю в `MessageHandler`
- [x] Добавить system prompt в начало каждого запроса к LLM
- [x] Сохранять сообщения пользователя и ассистента в историю

**Тест:** Бот помнит контекст диалога. Можно задать вопрос, а затем уточняющий вопрос, и бот поймет контекст.

---

## Итерация 5: Команда очистки истории

**Цель:** Пользователь может очистить историю диалога

**Задачи:**
- [x] Реализовать метод `clear_history()` в `ConversationManager`
- [x] Добавить обработчик команды `/clear` в `MessageHandler`
- [x] Добавить информацию о команде в сообщение `/start`
- [x] Обработать нетекстовые сообщения (фото, файлы, стикеры)

**Тест:** Команда `/clear` очищает историю. Следующее сообщение начинает новый контекст. Нетекстовые сообщения игнорируются с пояснением.

---

## Итерация 6: Обработка ошибок и финальная полировка

**Цель:** Устойчивая работа бота с корректной обработкой ошибок

**Задачи:**
- [x] Добавить обработку ошибок OpenRouter API в `LLMClient`
- [x] Добавить обработку ошибок Telegram API
- [x] Улучшить логирование (добавить контекст: user_id, длина сообщений)
- [x] Реализовать graceful shutdown (Ctrl+C)
- [x] Создать `README.md` с инструкциями по запуску

**Тест:** При ошибке API бот продолжает работать и отправляет понятное сообщение пользователю. Корректная остановка по Ctrl+C.

---

## Итерация 7: Ролевая модель с командой /role (TDD)

**Цель:** Превратить базового ассистента в ролевой AI-продукт с системным промптом из файла и командой отображения роли

**Подход:** Test-Driven Development (TDD) - сначала тесты, потом код, затем рефакторинг

**Задачи (TDD цикл):**

### 🔴 Фаза 1: Красная полоса - Тесты для загрузки промпта из файла

- [ ] **Создать структуру директорий:**
  - [ ] Создать `prompts/` директорию
  - [ ] Создать файл-пример `prompts/role_example.txt` с демо-ролью

- [ ] **Написать тесты для RoleManager** (`tests/test_role_manager.py`):
  - [ ] `test_load_role_from_file_success` - успешная загрузка роли из файла
  - [ ] `test_load_role_from_file_not_found` - обработка отсутствующего файла
  - [ ] `test_load_role_empty_file` - обработка пустого файла
  - [ ] `test_get_role_description` - получение описания роли для пользователя
  - [ ] `test_role_caching` - кэширование загруженной роли

- [ ] **Запустить тесты** - убедиться что все падают (красная полоса) ❌

### 🟢 Фаза 2: Зеленая полоса - Реализация загрузки промпта

- [ ] **Создать `src/bot/role_manager.py`:**
  - [ ] Класс `RoleManager` с методом `load_role(file_path: str) -> str`
  - [ ] Метод `get_role_description() -> str` для форматирования вывода
  - [ ] Обработка ошибок чтения файла
  - [ ] Кэширование загруженной роли

- [ ] **Обновить `src/bot/config.py`:**
  - [ ] Добавить параметр `ROLE_PROMPT_FILE` (путь к файлу с ролью)
  - [ ] Значение по умолчанию: `prompts/role.txt`

- [ ] **Запустить тесты** - убедиться что все проходят (зеленая полоса) ✅

### 🔴 Фаза 3: Красная полоса - Тесты для команды /role

- [ ] **Написать тесты для команды /role** (`tests/test_handlers.py`):
  - [ ] `test_handle_role_command_success` - успешное отображение роли
  - [ ] `test_handle_role_command_formats_nicely` - красивое форматирование с эмодзи
  - [ ] `test_handle_role_command_with_role_manager_error` - обработка ошибки RoleManager

- [ ] **Запустить тесты** - убедиться что новые тесты падают (красная полоса) ❌

### 🟢 Фаза 4: Зеленая полоса - Реализация команды /role

- [ ] **Обновить `src/bot/handlers.py`:**
  - [ ] Добавить зависимость `role_manager: RoleManager` в `MessageHandler`
  - [ ] Реализовать обработчик команды `/role`
  - [ ] Форматировать вывод с эмодзи 🎭
  - [ ] Добавить обработку ошибок

- [ ] **Обновить `src/bot/bot.py`:**
  - [ ] Инициализировать `RoleManager` при создании бота
  - [ ] Передать `RoleManager` в `MessageHandler`
  - [ ] Зарегистрировать обработчик команды `/role`

- [ ] **Обновить `src/bot/__main__.py`:**
  - [ ] Убедиться что `RoleManager` инициализируется с путем из конфигурации

- [ ] **Запустить тесты** - убедиться что все проходят (зеленая полоса) ✅

### 🔴 Фаза 5: Красная полоса - Тесты для использования роли в LLM

- [ ] **Написать тесты** (`tests/test_llm_client.py`):
  - [ ] `test_generate_response_uses_role_from_file` - LLM использует роль из файла вместо хардкода
  - [ ] `test_system_prompt_includes_role` - системный промпт содержит загруженную роль

- [ ] **Запустить тесты** - убедиться что новые тесты падают (красная полоса) ❌

### 🟢 Фаза 6: Зеленая полоса - Интеграция роли в LLM

- [ ] **Обновить `src/bot/llm_client.py`:**
  - [ ] Удалить хардкод системного промпта из `Config`
  - [ ] Принимать `system_prompt: str` в методе `generate_response()`
  - [ ] Использовать переданный системный промпт

- [ ] **Обновить `src/bot/handlers.py`:**
  - [ ] Получать роль из `RoleManager` перед вызовом LLM
  - [ ] Передавать роль как системный промпт в `generate_response()`

- [ ] **Запустить тесты** - убедиться что все проходят (зеленая полоса) ✅

### 🔵 Фаза 7: Рефакторинг и финализация

- [ ] **Рефакторинг:**
  - [ ] Проверить на дублирование кода
  - [ ] Улучшить именование методов и переменных
  - [ ] Применить SOLID принципы
  - [ ] Упростить сложную логику

- [ ] **Создать файл роли по умолчанию:**
  - [ ] Создать `prompts/role.txt` с базовой ролью ассистента
  - [ ] Документировать структуру файла роли в комментариях

- [ ] **Обновить документацию:**
  - [ ] Обновить `README.md` с описанием команды `/role`
  - [ ] Добавить инструкции по кастомизации роли
  - [ ] Обновить `.env.example` с переменной `ROLE_PROMPT_FILE`
  - [ ] Обновить приветственное сообщение `/start` с упоминанием `/role`

- [ ] **Запустить полную проверку:**
  - [ ] `make test` - все тесты зеленые
  - [ ] `make lint` - нет замечаний линтера
  - [ ] `make format` - код отформатирован
  - [ ] Coverage >80% для новых модулей

**Тест итерации:** 
1. Команда `/role` отображает роль бота с красивым форматированием (эмодзи 🎭)
2. Изменение файла `prompts/role.txt` меняет поведение бота без перезапуска (если реализовано кэширование с TTL) или после перезапуска
3. Бот использует роль из файла в диалогах с LLM
4. Все тесты зеленые, coverage >80% для `role_manager.py`
5. `make qa` проходит без ошибок

**Критерии завершения:**
- ✅ Все тесты написаны и зеленые
- ✅ RoleManager загружает промпт из файла
- ✅ Команда `/role` работает корректно
- ✅ LLM использует роль из файла вместо хардкода
- ✅ Код отрефакторен и следует SOLID
- ✅ Coverage >80% для `role_manager.py`
- ✅ Документация обновлена

---

## Готово! 🎉

После завершения всех итераций у вас будет полнофункциональный ролевой AI-ассистент с четкой специализацией.

